# Google Apps Script Web App URL.
# Deploy google-apps-script/Code.gs as a web app (Anyone, even anonymous),
# then paste the /exec URL here. Leave blank for DEMO_MODE (forms simulate success).
# See google-apps-script/README.md for deployment steps.
VITE_SCRIPT_URL=
